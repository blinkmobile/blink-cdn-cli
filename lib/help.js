module.exports = `
  Usage: bm cdn <command>

  Where command is one of:

  scope

Initial settings:
    scope                     => outputs the current scope
    scope [<s3 bucket name>]  => sets the current URL scope

`;
